To report a requester, we need you to be logged in. If you've never signed into Turkopticon before, #{link_to 'you can create a login.', controller: 'reg', action: 'register'}

.box{:style => "margin-top:15px"}
  = form_tag do
    %p
      .label Email Address
      = text_field_tag 'email'

    %p
      .label Password
      = password_field_tag 'password'

    %p
      .label &nbsp;
      = link_to "Forgot your password?", :action => "reset_password"

    %p
      Don't have a Turkopticon password? #{link_to 'Create a login', controller: 'reg', action: 'register'}

    %p
      .button= submit_tag 'Login'

    %p
      <img height=1 />
