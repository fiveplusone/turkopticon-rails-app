!!!
%html{html_attrs('en-US')}
  %head
    %title Turkopticon Moderation
    = stylesheet_link_tag 'moderation'
    = javascript_include_tag :partial_replacer
  %body
    #header
      #login_nav= render :partial => 'login_nav'
      #title Turkopticon Moderation
    #nav= render :partial => 'nav'
    #flash_messages
      - if flash[:success]
        .success= flash[:success].html_safe
      - if flash[:notice]
        .notice= flash[:notice].html_safe
      - if flash[:warning]
        .warning= flash[:warning].html_safe
      - if flash[:error]
        .error= flash[:error].html_safe
    #main= yield
